Ext.define('criterion.store.Base', function() {

    var API = criterion.consts.Api.API;

    return {
        alias : 'store.criterion_base',

        extend : 'criterion.store.AbstractStore'
    };
});